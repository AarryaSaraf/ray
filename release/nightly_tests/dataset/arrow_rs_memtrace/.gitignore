# Regenerable benchmark scratch — never commit.
# Fixtures (26 GB of parquet), per-worker USS traces, and generated figures are
# all produced by bench_suite.py / summarize.py and should stay local.
data/
runs/
figs/
__pycache__/
*.pyc
# Full-suite run logs + the pointer file summarize.py drops — regenerable.
*.log
.last_full_run_log
# Ad-hoc fixture dirs the leak-sweep scripts (gen.py / sweep.sh) write.
leak_linux_data/
